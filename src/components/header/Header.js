import React from 'react';
import s from "./Header.module.css";


const Header = (props) => {
    return (
        <div className={s.header}>
            <h1>HEADER</h1>
        </div>
    )
};

export default Header;